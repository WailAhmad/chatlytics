# Optional: enables LLM-backed planning for open-ended questions.
# The four assessment-critical questions are handled by deterministic fallback
# rules and do not require this key.
GROQ_API_KEY=your_groq_api_key_here
